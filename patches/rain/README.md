# OP7 patch set — rain bundle (TypeScript client core)

Applied over the pinned rain commit (`upstream/rain/commit.txt`) before
`bun run build`. r1: no source changes — the bundle is built as upstream
Forgejo CI does (`--release-branch=main --build-bytecode`).

| Patch | Change | Reason | Baseline | Result | Status |
|---|---|---|---|---|---|
| (none, r1) | unmodified upstream | baseline | — | — | KEEP |
